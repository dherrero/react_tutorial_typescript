import React from "react";
import { Game } from "../components";

const Home = () => {
    return <div>
        <h1>React Tutorial with TypeScript, Funcion Components and Hooks</h1>
        <Game />
    </div>
}

export default Home;